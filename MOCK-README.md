# 🎭 Langflow Mock - Инструкция по использованию

Заглушка для тестирования UI генератора тестов без реального обращения к Langflow API.

## 📦 Что включено

**Mock данные содержат:**
- ✅ **5 полноценных тестов** с подробными шагами:
  - TC-001: Создание нового пользователя
  - TC-002: Редактирование профиля пользователя
  - TC-003: Поиск и фильтрация по каталогу
  - TC-004: Оформление заказа с несколькими товарами
  - TC-005: Восстановление пароля через email

- ✅ **7 дополнительных проверок**:
  - CHECK-001: Валидация форм
  - CHECK-002: Безопасность и авторизация
  - CHECK-003: Производительность
  - CHECK-004: Кросс-браузерная совместимость
  - CHECK-005: Адаптивность (Responsive Design)
  - CHECK-006: Доступность (Accessibility - A11y)
  - CHECK-007: Offline функциональность

## 🚀 Способ использования

### Вариант 1: Добавление параметра в URL (Рекомендуется)

1. **Подключите mock скрипт** к вашему HTML файлу `test-gen-ui.html`:
   ```html
   <head>
       ...
       <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
       <!-- Добавьте эту строку ПЕРЕД закрывающим тегом </head> -->
       <script src="langflow-mock.js"></script>
   </head>
   ```

2. **Откройте страницу с параметром `?mock=true`**:
   ```
   file:///path/to/test-gen-ui.html?mock=true
   ```
   или если используете локальный сервер:
   ```
   http://localhost:8000/test-gen-ui.html?mock=true
   ```

3. **Готово!** Теперь все запросы к Langflow будут перехвачены и заменены на mock данные.

### Вариант 2: Временное подключение через консоль браузера

Если вы не хотите изменять оригинальный HTML файл:

1. Откройте `test-gen-ui.html` в браузере
2. Откройте консоль разработчика (F12)
3. Вставьте содержимое файла `langflow-mock.js`
4. Нажмите Enter
5. Обновите страницу с параметром `?mock=true`

## 🎯 Использование

1. **Откройте страницу с mock режимом**
   - В правом верхнем углу появится оранжевый индикатор **🎭 MOCK MODE**

2. **Заполните форму** (можно указать любые значения):
   - Добавьте любые URL в поля (они не будут использоваться)
   - Например: `https://example.com/page1`

3. **Нажмите "Сгенерировать тесты"**
   - Появится лоадер на 0.5-1.5 секунды (имитация сетевой задержки)
   - Отобразятся 5 тестов с подробными шагами
   - Отобразятся 7 дополнительных проверок

4. **Тестируйте UI**:
   - ✅ Разворачивание/сворачивание тестов
   - ✅ Выбор тестов через чекбоксы
   - ✅ Копирование тестов
   - ✅ Отправка в JIRA (тоже работает через mock)

## 🔧 Настройки

Для отключения mock режима просто уберите параметр `?mock=true` из URL или удалите подключение скрипта.

## 📝 Что перехватывается

Mock скрипт перехватывает:
- ✅ Запросы на генерацию тестов (основной endpoint)
- ✅ Запросы на отправку тестов в JIRA
- ❌ **НЕ перехватывает** другие запросы (например, к CDN для библиотек)

## 🐛 Отладка

Откройте консоль браузера (F12) для просмотра логов:

```
🎭 Langflow Mock Server активирован
📤 Mock: Перехвачен запрос к Langflow
🧪 Mock: Обработка запроса генерации тестов
✅ Mock: Возвращаем mock данные
```

## ⚠️ Важно

- Mock не изменяет оригинальный код страницы
- Mock работает только в браузере (client-side)
- Для отключения достаточно убрать `?mock=true` из URL
- Все данные статичны и не зависят от введенных значений

## 📚 Структура mock данных

Mock возвращает данные в том же формате, что и реальный Langflow:

```json
{
  "outputs": [{
    "outputs": [{
      "results": {
        "message": {
          "text": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<tests>...</tests>"
        }
      }
    }]
  }]
}
```

XML содержит:
- `<tests>` - список тестов с атрибутами `id`
- `<additional_checks>` - дополнительные проверки с атрибутами `id`

## 🎨 Пример использования

1. Подключите mock:
   ```html
   <script src="langflow-mock.js"></script>
   ```

2. Откройте с параметром:
   ```
   test-gen-ui.html?mock=true
   ```

3. Заполните любые данные и нажмите "Сгенерировать"

4. Наслаждайтесь полным UI без настройки Langflow! 🎉

## 💡 Подсказки

- Mock режим отображается индикатором в правом верхнем углу
- В консоли видны все перехваченные запросы
- Имитируется реалистичная задержка сети (500-1500мс)
- JIRA отправка тоже работает через mock (всегда успешно)
