# 🎭 Langflow Mock - Инструкция по использованию

Заглушка для тестирования UI генератора тестов без реального обращения к Langflow API.

**Важно:** Мок не изменяет оригинальный файл `test-gen-ui.html`! Это отдельный подключаемый скрипт.

## 📦 Что включено

**Mock данные содержат:**
- ✅ **5 полноценных тестов** с подробными шагами:
  - TC-001: Создание нового пользователя
  - TC-002: Редактирование профиля пользователя
  - TC-003: Поиск и фильтрация по каталогу
  - TC-004: Оформление заказа с несколькими товарами
  - TC-005: Восстановление пароля через email

- ✅ **7 дополнительных проверок**:
  - CHECK-001: Валидация форм
  - CHECK-002: Безопасность и авторизация
  - CHECK-003: Производительность
  - CHECK-004: Кросс-браузерная совместимость
  - CHECK-005: Адаптивность (Responsive Design)
  - CHECK-006: Доступность (Accessibility - A11y)
  - CHECK-007: Offline функциональность

## 🚀 Способы использования

### Способ 1: Через консоль браузера (Рекомендуется - без изменения файлов!)

1. **Откройте `test-gen-ui.html` в браузере**

2. **Откройте консоль разработчика** (F12 → Console)

3. **Скопируйте и вставьте содержимое файла `langflow-mock.js`** в консоль и нажмите Enter

4. **Обновите страницу с параметром `?mock=true`**:
   ```
   file:///path/to/test-gen-ui.html?mock=true
   ```

5. **Готово!** Появится индикатор 🎭 MOCK MODE и все запросы будут перехвачены

### Способ 2: Через bookmarklet (Одним кликом!)

1. **Создайте закладку в браузере** с таким содержимым:
   ```javascript
   javascript:(function(){var s=document.createElement('script');s.src='langflow-mock.js';document.head.appendChild(s);setTimeout(function(){location.search='?mock=true'},500);})();
   ```

2. **Откройте `test-gen-ui.html`**

3. **Кликните по закладке** - mock подключится автоматически!

### Способ 3: Локальный веб-сервер

Если у вас запущен локальный веб-сервер:

1. **Запустите простой HTTP сервер** в директории проекта:
   ```bash
   # Python 3
   python3 -m http.server 8000

   # Node.js (если установлен http-server)
   npx http-server -p 8000
   ```

2. **Откройте в браузере**:
   ```
   http://localhost:8000/test-gen-ui.html
   ```

3. **Откройте консоль (F12)** и вставьте код подключения mock:
   ```javascript
   var script = document.createElement('script');
   script.src = 'langflow-mock.js';
   document.head.appendChild(script);
   setTimeout(() => location.search = '?mock=true', 500);
   ```

### Способ 4: Временное изменение HTML (если нужно для демо)

Если нужно временно добавить mock для демонстрации:

1. **Откройте `test-gen-ui.html` в редакторе**

2. **Найдите строку**:
   ```html
   <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
   ```

3. **Добавьте ПОСЛЕ неё**:
   ```html
   <script src="langflow-mock.js"></script>
   ```

4. **Откройте в браузере с `?mock=true`**

5. **После тестирования удалите эту строку!**

## 🎯 Использование

1. **Убедитесь, что mock активирован**
   - В правом верхнем углу должен быть индикатор **🎭 MOCK MODE**
   - В консоли должно быть сообщение: "🎭 Langflow Mock Server активирован"

2. **Заполните форму** (можно указать любые значения):
   - Добавьте любые URL в поля (они не будут использоваться)
   - Например: `https://example.com/page1`

3. **Нажмите "Сгенерировать тесты"**
   - Появится лоадер на 0.5-1.5 секунды (имитация сетевой задержки)
   - Отобразятся 5 тестов с подробными шагами
   - Отобразятся 7 дополнительных проверок

4. **Тестируйте UI**:
   - ✅ Разворачивание/сворачивание тестов
   - ✅ Выбор тестов через чекбоксы
   - ✅ Копирование тестов
   - ✅ Отправка в JIRA (тоже работает через mock)

## 🔧 Настройки

### Включение mock режима
Mock активируется только при наличии параметра `?mock=true` в URL.

### Отключение mock режима
- Уберите параметр `?mock=true` из URL
- Или перезагрузите страницу без параметра

### Изменение mock данных
Откройте файл `langflow-mock.js` и найдите переменную `mockTestsResponse`. Измените XML содержимое по своему усмотрению.

## 📝 Что перехватывается

Mock скрипт перехватывает:
- ✅ Запросы на генерацию тестов (основной endpoint)
- ✅ Запросы на отправку тестов в JIRA
- ❌ **НЕ перехватывает** другие запросы (например, к CDN для библиотек)

## 🐛 Отладка

Откройте консоль браузера (F12) для просмотра логов:

```
🎭 Langflow Mock Server активирован
📤 Mock: Перехвачен запрос к Langflow
🧪 Mock: Обработка запроса генерации тестов
✅ Mock: Возвращаем mock данные
```

## ⚠️ Важно

- ✅ Mock НЕ изменяет оригинальный файл `test-gen-ui.html`
- ✅ Mock работает только в браузере (client-side)
- ✅ Для отключения достаточно убрать `?mock=true` из URL
- ✅ Все данные статичны и не зависят от введенных значений
- ✅ Безопасно для использования - не отправляет данные никуда

## 📚 Структура mock данных

Mock возвращает данные в том же формате, что и реальный Langflow:

```json
{
  "outputs": [{
    "outputs": [{
      "results": {
        "message": {
          "text": "<?xml version=\"1.0\" encoding=\"UTF-8\"?>\n<tests>...</tests>"
        }
      }
    }]
  }]
}
```

XML содержит:
- `<tests>` - список тестов с атрибутами `id`
- `<additional_checks>` - дополнительные проверки с атрибутами `id`

## 🎨 Быстрый старт для нетехнических пользователей

### Если вы просто хотите посмотреть как работает UI:

1. Откройте файл `test-gen-ui.html` двойным кликом (откроется в браузере)
2. Нажмите F12 на клавиатуре
3. Нажмите на вкладку "Console" в открывшемся окне
4. Скопируйте **ВЕСЬ** текст из файла `langflow-mock.js`
5. Вставьте в консоль и нажмите Enter
6. В адресной строке браузера в конце URL добавьте `?mock=true` и нажмите Enter
7. Увидите оранжевый индикатор 🎭 MOCK MODE
8. Нажмите кнопку "Сгенерировать тесты"
9. Готово! 🎉

## 💡 Подсказки

- Mock режим отображается индикатором в правом верхнем углу
- В консоли видны все перехваченные запросы
- Имитируется реалистичная задержка сети (500-1500мс)
- JIRA отправка тоже работает через mock (всегда успешно)
- Можно использовать для демонстраций и обучения

## ❓ Частые вопросы

### Q: Изменяется ли оригинальный файл?
**A:** Нет! `test-gen-ui.html` остается без изменений.

### Q: Нужно ли запускать сервер?
**A:** Нет, можно открывать файл напрямую через file://

### Q: Работает ли это без интернета?
**A:** Да, после первой загрузки (для шрифтов и библиотек).

### Q: Можно ли использовать в продакшене?
**A:** Нет, это только для тестирования UI. Для продакшена нужен реальный Langflow.

### Q: Безопасно ли это?
**A:** Да, mock работает только локально в браузере и не отправляет данные никуда.
