# 🚀 Быстрый старт с Langflow Mock

## 📁 Файлы в проекте

### Основные файлы:
- **`test-gen-ui.html`** - Оригинальная страница генератора тестов (не изменяется!)
- **`langflow-mock.js`** - Подключаемый скрипт-заглушка для имитации Langflow API

### Документация:
- **`MOCK-README.md`** - Подробная инструкция по использованию mock
- **`QUICK-START.md`** - Этот файл (быстрый старт)

---

## ⚡ Самый быстрый способ (через консоль браузера)

### 4 простых шага:

1. **Откройте `test-gen-ui.html`** двойным кликом (откроется в браузере)

2. **Откройте консоль** - нажмите клавишу **F12**, выберите вкладку **Console**

3. **Скопируйте всё содержимое файла `langflow-mock.js`** и вставьте в консоль, нажмите Enter

4. **Добавьте `?mock=true` в URL** и нажмите Enter
   ```
   file:///path/to/test-gen-ui.html?mock=true
   ```

**Готово!** Вы увидите оранжевый индикатор **🎭 MOCK MODE**

---

## 🎯 Что вы увидите

После нажатия кнопки "Сгенерировать тесты":

### 5 подробных тестов:
1. **TC-001**: Создание нового пользователя (с 5 шагами)
2. **TC-002**: Редактирование профиля пользователя (с 8 шагами)
3. **TC-003**: Поиск и фильтрация по каталогу (с 6 шагами)
4. **TC-004**: Оформление заказа с несколькими товарами (с 8 шагами)
5. **TC-005**: Восстановление пароля через email (с 8 шагами)

### 7 дополнительных проверок:
1. **CHECK-001**: Валидация форм
2. **CHECK-002**: Безопасность и авторизация
3. **CHECK-003**: Производительность
4. **CHECK-004**: Кросс-браузерная совместимость
5. **CHECK-005**: Адаптивность
6. **CHECK-006**: Доступность (A11y)
7. **CHECK-007**: Offline функциональность

---

## 🔄 Альтернативные способы

### Способ 2: Через локальный сервер

Если у вас есть Python или Node.js:

```bash
# Python 3
python3 -m http.server 8000

# Затем откройте: http://localhost:8000/test-gen-ui.html
```

В консоли браузера выполните:
```javascript
var s = document.createElement('script');
s.src = 'langflow-mock.js';
document.head.appendChild(s);
setTimeout(() => location.search = '?mock=true', 500);
```

### Способ 3: Временное изменение HTML (для демо)

**Только если нужно для демонстрации:**

1. Откройте `test-gen-ui.html` в редакторе
2. Найдите: `<script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>`
3. Добавьте после неё: `<script src="langflow-mock.js"></script>`
4. Откройте в браузере с `?mock=true`
5. **После тестирования удалите добавленную строку!**

---

## 🎨 Возможности UI для тестирования

✅ **Разворачивание/сворачивание тестов**
- Кликайте по заголовку теста
- Или используйте кнопку "Развернуть/Свернуть все"

✅ **Выбор тестов**
- Чекбоксы для отметки нужных тестов
- Кнопка "Выбрать все"

✅ **Копирование**
- Кнопка "📋 Копировать" на каждом тесте
- Кнопка "📋 Копировать" для дополнительных проверок

✅ **Отправка в JIRA** (тоже через mock!)
- Выберите тесты
- Заполните поля (любые значения)
- Нажмите "Отправить в JIRA"
- Mock вернет успешный результат

---

## 🔍 Проверка работы mock

### Индикатор Mock режима:
В правом верхнем углу появится оранжевый бадж:
```
🎭 MOCK MODE
```

### Консоль браузера (F12):
```
🎭 Langflow Mock Server активирован
📤 Mock: Перехвачен запрос к Langflow
🧪 Mock: Обработка запроса генерации тестов
✅ Mock: Возвращаем mock данные
```

---

## ❓ Частые вопросы

### Q: Изменяется ли оригинальный HTML?
**A:** Нет! Файл `test-gen-ui.html` остается без изменений.

### Q: Нужно ли заполнять поля формы?
**A:** Можно указать любые значения - mock их не использует.

### Q: Работает ли отправка в JIRA?
**A:** Да! Mock перехватывает и эти запросы, всегда возвращая успех.

### Q: Как отключить mock?
**A:** Уберите `?mock=true` из URL или закройте и откройте страницу заново.

### Q: Изменяются ли данные при повторной генерации?
**A:** Нет, mock всегда возвращает одинаковые данные (статичные).

### Q: Можно ли изменить mock данные?
**A:** Да! Откройте `langflow-mock.js` и измените переменную `mockTestsResponse`.

### Q: Нужен ли интернет?
**A:** Только для первой загрузки (шрифты, библиотеки). Потом можно работать offline.

---

## 🎓 Пошаговая инструкция для новичков

**Шаг 1:** Найдите файл `test-gen-ui.html` и откройте его (двойной клик мышкой)

**Шаг 2:** Нажмите клавишу **F12** на клавиатуре (откроется панель разработчика)

**Шаг 3:** Если не видите вкладку "Console" - найдите её вверху панели и кликните

**Шаг 4:** Откройте файл `langflow-mock.js` в блокноте, выделите весь текст (Ctrl+A), скопируйте (Ctrl+C)

**Шаг 5:** Вернитесь в браузер, кликните в консоль и вставьте (Ctrl+V), нажмите Enter

**Шаг 6:** Посмотрите на адресную строку браузера - там будет что-то вроде:
```
file:///C:/Users/YourName/Desktop/test-gen-ui.html
```

**Шаг 7:** Кликните в конец этой строки и добавьте: `?mock=true`

Должно получиться:
```
file:///C:/Users/YourName/Desktop/test-gen-ui.html?mock=true
```

**Шаг 8:** Нажмите Enter. Страница перезагрузится и в правом верхнем углу появится оранжевый значок 🎭 MOCK MODE

**Шаг 9:** Теперь просто нажмите большую синюю кнопку "🚀 Сгенерировать тесты"

**Шаг 10:** Наслаждайтесь результатом! 🎉

---

## 🛠️ Технические детали

- **Язык:** JavaScript (ES6+)
- **Совместимость:** Все современные браузеры
- **Размер:** ~30KB
- **Зависимости:** Нет (только нативные браузерные API)
- **Безопасность:** Работает только локально, не отправляет данные

---

## 💡 Советы по использованию

1. **Для быстрого тестирования** - используйте консоль браузера (Способ 1)
2. **Для демонстраций** - временно добавьте скрипт в HTML (Способ 3)
3. **Для разработки** - используйте локальный сервер (Способ 2)
4. **Для кастомизации** - редактируйте `langflow-mock.js`

---

## 🐛 Что делать если не работает?

### Проблема: Не появляется индикатор 🎭 MOCK MODE

**Решение:**
1. Проверьте, что в URL есть `?mock=true`
2. Откройте консоль (F12) и проверьте наличие ошибок
3. Убедитесь, что скрипт был вставлен полностью

### Проблема: Ошибка в консоли "Uncaught SyntaxError"

**Решение:**
1. Убедитесь, что скопировали **ВСЁ** содержимое `langflow-mock.js`
2. Попробуйте скопировать заново
3. Проверьте, что файл не поврежден

### Проблема: Тесты не генерируются

**Решение:**
1. Проверьте наличие индикатора 🎭 MOCK MODE
2. Откройте консоль и посмотрите логи
3. Попробуйте перезагрузить страницу с mock заново

---

## 🎁 Бонус: Создание bookmarklet

Создайте закладку в браузере для быстрого подключения mock:

1. Создайте новую закладку
2. В поле URL вставьте:
```javascript
javascript:(function(){var s=document.createElement('script');s.src='langflow-mock.js';document.head.appendChild(s);setTimeout(function(){location.search='?mock=true'},500);})();
```
3. Назовите её "🎭 Enable Mock"
4. Теперь просто кликайте по ней когда нужен mock!

---

**Приятного тестирования!** 🎉

Для подробностей смотрите **`MOCK-README.md`**
